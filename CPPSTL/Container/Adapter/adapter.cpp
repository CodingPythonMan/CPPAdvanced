/*#include <iostream>
#include <stack>
#include <queue>
#include <vector>
#include <deque>
#include <list>

int main()
{
	std::stack<int> s1;
	std::stack<int, std::vector<int>> s2;
	std::stack<int, std::list<int>> s3;
	s1.push(10);
	s1.pop();
	s1.top();

	std::queue<int> q1;
	std::queue<int, std::vector<int>> q2;
	std::queue<int, std::list<int>> q3;

	q1.push(10);
	q2.push(10);	// push ´Â push_back È£Ãâ
	q1.pop();		// ok
	//q2.pop();		// error
	q1.front();

	std::priority_queue<int> pq;
}*/