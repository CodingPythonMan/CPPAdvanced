/*#include <iostream>
#include <list>
#include <stack>
#include <set>

struct Point
{
	int x;
	int y;
};

int main()
{
	//std::list<Point> s;
	//Point pt;
	//s.push_back(pt);

	//std::list<Point>::value_type n; // Point
	//std::list<Point>::iterator p; // 반복자 타입

	//auto p = s.begin();
	//auto p1 = begin(s);
	
	std::list<int> s;
	
	s.push_back(10);
	s.push_back(20);
	s.push_back(30);

	std::cout << s.back() << std::endl; // 30
	s.pop_back();
	std::cout << s.back() << std::endl; // 30
}*/